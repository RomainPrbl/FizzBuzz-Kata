#include stdio.h
#include stdlib.h

int main (void) {
	printf("hello");
    for (int i = 1; i = 100; i++) {
        if (i % 15 == 0) {
            printf (FizzBuzzn);
        } else if (i % 3 == 0) {
            printf (UwUn);
        } else if (i % 5 == 0) {
            printf (Buzzn);
        } else {
            printf (%dn, i);
        }
    }
    return EXIT_SUCCESS;
} 
